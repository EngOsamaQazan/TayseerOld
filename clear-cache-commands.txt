# أوامر مسح الكاش - انسخها والصقها بعد الاتصال بالسيرفر
# الاتصال: ssh root@54.38.236.112
# كلمة المرور: Hussain@1986

# بعد الدخول، نفّذ الأوامر التالية (عدّل المسار إذا كان مختلفاً):

cd /var/www/html || cd /var/www || cd /home

# ابحث عن مجلد التطبيق
ls -la

# ثم ادخل لمجلد jadal أو التطبيق واستبدل APP_PATH في الأوامر التالية:
APP_PATH="/var/www/html"   # عدّل هذا حسب موقع التطبيق

cd $APP_PATH

# مسح كاش Yii2
rm -rf backend/runtime/cache/*
rm -rf backend/runtime/logs/*
rm -rf frontend/runtime/cache/* 2>/dev/null
rm -rf console/runtime/cache/* 2>/dev/null
find backend/runtime -mindepth 1 ! -name '.gitignore' -exec rm -rf {} + 2>/dev/null

# مسح الكاش عبر Yii إن وجد
php yii cache/flush-all 2>/dev/null

echo "تم مسح الكاش"
