# ═══════════════════════════════════════════════════════════════
#  إعدادات النشر — انسخ هذا الملف إلى deploy.config واملأ القيم
#  cp deploy.config.example deploy.config
# ═══════════════════════════════════════════════════════════════

# ── السيرفر المشترك (إن كان namaa و jadal على نفس السيرفر) ──
SERVER_HOST="54.38.236.112"
SERVER_USER="root"

# طريقة المصادقة: إما كلمة مرور أو مفتاح SSH
# اترك SSH_KEY فارغاً لاستخدام كلمة المرور
SSH_KEY=""
# أو استخدم مفتاحاً: SSH_KEY="$HOME/.ssh/id_rsa"

# كلمة المرور (تُستخدم فقط إذا كان SSH_KEY فارغاً)
# تحذير: لا ترفع deploy.config إلى Git
SSH_PASS=""

# ── مسارات المشروع على السيرفر ──
# مسار مجلد جدل (jadal.aqssat.co)
JADAL_PATH="/var/www/jadal"

# مسار مجلد نماء (namaa.aqssat.co)
NAMAA_PATH="/var/www/namaa"

# ── إذا كان كل موقع على سيرفر مختلف، أضف: ──
# JADAL_HOST="ip_سيرفر_جدل"
# JADAL_USER="root"
# NAMAA_HOST="ip_سيرفر_نماء"
# NAMAA_USER="root"

# ── فرع Git للنشر ──
GIT_BRANCH="main"
